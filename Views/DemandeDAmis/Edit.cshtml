@model NetAtlas.Models.DemandeDAmis

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DemandeDAmis</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
               
                <input asp-for="Amis1ID" type="hidden" class="form-control" />
                
            <div class="form-group">
                
                <input asp-for="Amis2ID" class="form-control" type="hidden" >
               
            </div>
            <div class="form-group">
                <label asp-for="Statut" class="control-label"></label>
                <select asp-for="Statut" class="form-control" />
                <option>Accepté</option>
                 <option>Refusé</option>
                 <span asp-validation-for="Statut" class="text-danger"></span>
                 </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
